#!/usr/bin/env python3

import libwyag
libwyag.main()

# This script serves as the entry point for the wyag application.
# It imports the main function from the libwyag module and executes it.
# Ensure that the libwyag module is in the Python path or the same directory as this script.
# You can run this script directly to start the wyag application.